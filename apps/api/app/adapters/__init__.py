"""Adapter layer — ports and implementations for external concerns.

Adapters isolate infrastructure details (embedding models, vector stores,
file parsers, etc.) behind protocol interfaces so that services depend
on abstractions, not concrete libraries.
"""
